{% extends 'SelfFrontBundle::layout.html.twig' %}

 {% block content %}
     <img src="{{ asset('uploads/logos/' ~ team.path) }}" />
     <h1>{{ team.name }}</h1>
     <p> Website : <a href="{{ team.websiteUrl }}"> {{ team.websiteUrl }}</a></p>

 {% endblock %}